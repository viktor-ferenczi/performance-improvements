<!-- ReSharper disable once Xaml.RedundantNamespaceAlias -->
<UserControl x:Class="TorchPlugin.ConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:TorchPlugin"
             xmlns:viewModels="clr-namespace:TorchPlugin.ViewModels"
             xmlns:patching="clr-namespace:Shared.Patches.Patching"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800" d:DataContext="{viewModels:ConfigViewModel}">

    <StackPanel Orientation="Vertical">
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="Performance Improvements" FontWeight="Bold" FontSize="16" VerticalAlignment="Center" Margin="5" />
            <Button Content="Save" Margin="5" Width="50" Click="SaveOnClick" />
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <CheckBox IsChecked="{Binding Enabled}" Margin="5" />
            <TextBlock Text="Enabled" VerticalAlignment="Center" Margin="5" />
        </StackPanel>

        <TextBlock Text="Fixes" FontWeight="Bold" FontSize="16" VerticalAlignment="Center" Margin="5" />
        
        <TreeView ItemsSource="{Binding InfoTree.Root.Values, Mode=OneTime}" Margin="3">
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate DataType="{x:Type patching:PatchInfoTree+Node}" ItemsSource="{Binding Children.Values, Mode=OneTime}">
                    <StackPanel Orientation="Horizontal" Margin="3">
                        <CheckBox Focusable="False" IsChecked="{Binding Enabled}" />
                        <TextBlock Text="{Binding DisplayName, Mode=OneTime}" Margin="3, 0, 0, 0" />
                    </StackPanel>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>
    </StackPanel>
</UserControl>